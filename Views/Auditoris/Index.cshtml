@model IEnumerable<Sekretari_Demo.Models.Auditori>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:orangered"><strong>Auditor</strong></h2>
<div>
    Kerko Auditor: <input id="search" />
    <button onclick="search()">search</button>
</div>
<div><br /></div>
<p>
    @Html.ActionLink("Krijo te re", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Klasa.Grupi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mesuesi.emer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nxene.emer)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Klasa.Grupi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mesuesi.emer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nxene.emer)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_auditori }, new { @class = "btn btn-success" }) |
                @Html.ActionLink("Details", "Details", new { id = item.id_auditori }, new { @class = "btn btn-info" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id_auditori }, new { @class = "btn btn-danger deleteCategory" })
            </td>
        </tr>
    }

</table>
